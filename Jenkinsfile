pipeline {
    agent any
    environment {
        BRANCH_NAME = "${env.BRANCH_NAME}"
    }
    environment {
        BRANCH_NAME = "${env.BRANCH_NAME}"
    }
    stages {
        stage('Build') {
            steps {
                // Get some code from a GitHub repository
               echo "Hello Jenkins"
            }
        }
    }
}